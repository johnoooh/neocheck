streamlit>=1.32.0
nest_asyncio>=1.6
gradio>=4.44
pandas>=2.0.0
plotly>=5.18.0
requests>=2.33.0
anthropic>=0.40.0
transformers>=4.46
accelerate>=1.0
torch>=2.4
# Required to load cyankiwi/Qwen3.6-27B-AWQ-INT4 (DEFAULT_LOCAL_MODEL_ID).
# transformers dispatches to AWQ kernels automatically when autoawq is present.
autoawq>=0.2.7
huggingface_hub>=0.26
datasets>=3.0
spaces>=0.30
# pubmedmcp runtime deps (the package itself is run from mcp/pubmedmcp/src
# via `python -m pubmedmcp` with cwd set in MCP_SERVERS_CONFIG; HF Spaces'
# build context only has requirements.txt mounted, so we can't pip-install
# the local path here — only its deps).
mcp>=1.13.1
pubmedclient>=0.2.0
